import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        borderRadius: 10,
        width: 150,
        backgroundColor: '#c2eb47'
    },
    title: {
        fontSize: 22,
        padding: 12,
        color: 'white',
        fontWeight: '600',
        textAlign: 'center'
    }
});